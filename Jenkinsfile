pipeline {
    agent {
    stages {
        stage('Build') { 
            steps {
                sh 'cd /var/lib/jenkins/workspace/react-test2'
                sh 'npm start'
            }
        }
    }
}
